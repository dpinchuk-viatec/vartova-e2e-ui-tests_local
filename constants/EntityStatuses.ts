export enum MsgStatuses {
    Success = "SUCCESS",
}

export enum CodeStatuses {
    Zero = "0",
    "0x1e9601000" = "0x1e9601000",
}

export enum LoginStatuses {
    LoginUserExpired = "Login user expired.",
}